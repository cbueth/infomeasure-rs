# SPDX-FileCopyrightText: 2025-2026 Carlson Büth <code@cbueth.de>
#
# SPDX-License-Identifier: MIT OR Apache-2.0

workspace:
  base: /build
  path: .

labels:
  provider: self-hosted
  type: cpu

when:
  - event: [push, pull_request]
    branch: [main, develop]
    path:
      - Cargo.toml
      - Cargo.lock
      - src/**
      - tests/**
  - event: manual

matrix:
  RUST: [stable, beta]
  # `parallel` (rayon) is CPU-only, so the normal CPU runners exercise it here.
  # `gpu` is not tested in this workflow (no adapter) - see test-gpu.yml. Tests
  # deliberately run with rayon's default thread count (all cores); the timed
  # benchmark track is the one that pins a fixed number of threads.
  FEATURES: ["", "parallel"]

steps:
  - name: build
    image: codeberg.org/cbueth/infomeasure-rs:${RUST}
    environment:
      FEATURES: ${FEATURES}
      RUST: ${RUST}
      CARGO_HOME: /target/infomeasure-rs-cargo-${RUST}
      CARGO_TARGET_DIR: /target/infomeasure-rs-${RUST}
      CARGO_MANIFEST_DIR: /build/tests/validation_crate
    commands:
      - if [ ! -e /target/infomeasure-rs-${RUST}/.seeded ] && [ -d /usr/local/cargo/target ]; then
          mkdir -p /target/infomeasure-rs-${RUST} /target/infomeasure-rs-cargo-${RUST} &&
          cp -an /usr/local/cargo/target/. /target/infomeasure-rs-${RUST}/ &&
          cp -an /usr/local/cargo/. /target/infomeasure-rs-cargo-${RUST}/ &&
          touch /target/infomeasure-rs-${RUST}/.seeded;
        fi
      - if [ -n "${FEATURES}" ]; then
          cargo build -j 4 --workspace --features ${FEATURES};
        else
          cargo build -j 4 --workspace;
        fi

  - name: test
    image: codeberg.org/cbueth/infomeasure-rs:${RUST}
    environment:
      FEATURES: ${FEATURES}
      RUST: ${RUST}
      CARGO_HOME: /target/infomeasure-rs-cargo-${RUST}
      CARGO_TARGET_DIR: /target/infomeasure-rs-${RUST}
      CARGO_MANIFEST_DIR: /build/tests/validation_crate
    commands:
      - if [ -n "${FEATURES}" ]; then
          cargo test -j 4 --workspace --features ${FEATURES};
        else
          cargo test -j 4 --workspace;
        fi

  - name: doctest
    image: codeberg.org/cbueth/infomeasure-rs:${RUST}
    environment:
      CARGO_HOME: /target/infomeasure-rs-cargo-${RUST}
      CARGO_TARGET_DIR: /target/infomeasure-rs-${RUST}
      CARGO_MANIFEST_DIR: /build/tests/validation_crate
    commands:
      - cargo test --doc
